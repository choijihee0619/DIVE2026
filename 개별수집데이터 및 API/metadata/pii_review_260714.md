# RAG 청크 PII 재검수 리포트

작성일: 2026-07-14

대상: `processed/rag/rag_chunks_260714.jsonl` (총 1009건)

이 리포트는 사람 검수를 대체하지 않는다. 자유서술형 텍스트(질문/담당자의견/특이사항)에서
실명, 정황상 식별 가능한 준식별자까지는 정규식으로 완전히 잡아낼 수 없으므로,
아래 우선순위를 참고해 사람이 최종 확인 후 `pii_removed`를 `true`로 바꿔야 한다.

## 1. 탐지 결과 요약

| 패턴 | 탐지 건수 |
|---|---|
| phone_mobile | 0 |
| phone_landline | 0 |
| rrn | 0 |
| email | 0 |
| account_like | 0 |
| detailed_address | 0 |

- 패턴 탐지되어 **우선 검수 대상(high)**: 0건 / 1009건 (0.0%)
- 패턴 미검출(low, 그래도 검수 필요): 1009건

전화번호·주민번호·이메일·계좌의심패턴·상세주소(동/호/번지) 5종 모두 1,009건 전체에서 0건 검출됐다.
발제사가 이미 "비식별" 처리한 상담데이터라는 점과 부합한다. 다만 이는 하드 식별자가 없다는 뜻일 뿐,
자유서술형 문장 안의 실명·회사명·계약일+지역 조합 같은 준식별자는 정규식으로 잡히지 않으므로
`pii_removed`를 곧바로 `true`로 바꾸지 않았다.

## 2. 우선 검수 대상 (패턴 탐지 청크, 미리보기는 마스킹 처리)

패턴 탐지 청크가 0건이므로 표는 비어 있다. 대신 전수 검수 부담을 줄이기 위해
`situation_summary`·`manager_opinion`·`special_note` 결측이 아닌 청크 중 무작위 30건을
1차 표본 검수 대상으로 권장한다(전체 결과가 0건이라 하더라도 자유서술 텍스트는 표본 검수를 생략하지 않는다).

## 3. 처리 방법

1. 위 표의 chunk_id를 `rag_chunks_260714_reviewed.jsonl`에서 찾아 원문 확인.
2. 실제 개인정보이면 해당 chunk 재작성(마스킹) 또는 제외.
3. 사람 확인이 끝난 청크는 `metadata.pii_scan_v2.human_reviewed`를 `true`로,
   문제 없으면 `metadata.pii_removed`를 `true`로 갱신.
4. 모든 청크의 `pii_removed`가 `true`가 된 뒤에만 임베딩 파이프라인에 투입.

## 4. 알려진 한계

- 정규식은 전화번호/주민번호/이메일/계좌의심패턴/상세주소(동·호·번지)만 탐지한다.
- 실명, 회사명, 날짜+지역 조합에 의한 재식별 가능성은 이 스캔으로 검출되지 않는다.
- `account_like` 패턴은 오탐(금액 표기 등)이 섞일 수 있어 반드시 사람이 재확인해야 한다.