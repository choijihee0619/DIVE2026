import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

/** LAND-00 임대인 홈. 실제 화면 구현은 다음 단계(명세서 15장)에서 진행한다. */
export default function LandlordHomePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>임대인 홈</CardTitle>
      </CardHeader>
      <CardContent>보완요청 알림 목록은 다음 단계에서 연동됩니다.</CardContent>
    </Card>
  );
}
